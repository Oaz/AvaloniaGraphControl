<Window xmlns="https://github.com/avaloniaui" 
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
  xmlns:agc="clr-namespace:AvaloniaGraphControl;assembly=AvaloniaGraphControl" mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450" Width="1000" Height="400" x:Class="AvaloniaGraphControlSample.MainWindow" Title="AvaloniaGraphControlSample">
  <Grid ColumnDefinitions="1*" RowDefinitions="25,1*">
    <StackPanel Grid.Column="0" Grid.Row="0" Orientation="Horizontal">
      <TextBlock>Select Graph:</TextBlock>
      <ComboBox Name="graphs" Items="{Binding SampleGraphs}"/>
      <TextBlock>Select Zoom:</TextBlock>
      <Slider Name="zoomer" Orientation="Horizontal" Width="200" Value="1" Minimum="0.3" Maximum="3"/>
      <TextBlock>Select Layout Method:</TextBlock>
      <ComboBox Name="layoutMethods" />
    </StackPanel>
    <agc:GraphView Grid.Column="0" Grid.Row="1"
                   Source="{Binding SelectedItem.Graph, ElementName=graphs}"
                   Zoom="{Binding Value, ElementName=zoomer}"
                   LayoutMethod="{Binding SelectedItem, ElementName=layoutMethods}" />
  </Grid>
</Window>
